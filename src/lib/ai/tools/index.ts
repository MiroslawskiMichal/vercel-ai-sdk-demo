import { getUserOrders } from './orders-tool';
import { getCurrentTime } from './time-tool';
import { getWeather } from './weather-tool';

export const tools = {
  getCurrentTime,
  getWeather,
  getUserOrders,
};
